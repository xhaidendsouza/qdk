name: qdk-publish-$(BuildId)

# Run on merges to main to ensure that the latest code
# is always able to be published.
trigger:
  branches:
    include:
      - main

# Run the pipeline every day at 6:00 AM to ensure
# codeql and other governance checks are up-to-date.
schedules:
  - cron: "0 6 * * *"
    displayName: "Build for Component Governance"
    branches:
      include:
        - main
    always: true

variables:
  CARGO_TERM_COLOR: always
  RUST_TOOLCHAIN_VERSION: "1.95"

resources:
  repositories:
    - repository: 1ESPipelineTemplates
      type: git
      name: 1ESPipelineTemplates/1ESPipelineTemplates
      ref: refs/tags/release

parameters:
  - name: matrix
    type: object
    default:
      - name: linux_x86_64
        poolName: "Azure-Pipelines-DevTools-GPU"
        imageName: "ubuntu-2204-qdk-gpu"
        os: linux
        arch: x86_64
        envVars:
          QDK_GPU_TESTS: "1"
      - name: linux_aarch64
        poolName: "Azure-Pipelines-DevTools-ARM64-EO"
        imageName: "azurelinux-3.0-gen2-arm64"
        os: linux
        arch: aarch64
      - name: mac_x86_64
        poolName: "Azure Pipelines"
        imageName: "macOS-latest" # MacOS-specific Py (Mac is usually quite limited).
        os: macOS
        arch: x86_64
      - name: mac_aarch64
        poolName: "AcesShared"
        os: macOS
        arch: aarch64
        envVars:
          QDK_GPU_TESTS: "1"
      - name: windows_x86_64
        poolName: "Azure-Pipelines-DevTools-GPU"
        imageName: "1es-pt-dsvm-windows-2022" # Win-specific Py + Platform-independent Py.
        os: windows
        arch: x86_64
        envVars:
          QDK_GPU_TESTS: "1"
      - name: windows_aarch64
        poolName: "Azure-Pipelines-DevTools-ARM64-EO"
        imageName: "windows-2025-gen2-arm64"
        os: windows
        arch: aarch64

# variables set by pipeline
# - BASE_IMAGE
# - BUILD_NUMBER
# - BUILD_TYPE
# - cratesIoFeedOverride
# - OwnerPersonalAlias
# - toolchainFeed

extends:
  template: v1/1ES.Official.PipelineTemplate.yml@1ESPipelineTemplates
  parameters:
    sdl:
      sourceAnalysisPool:
        name: "Azure-Pipelines-DevTools-EO"
        image: windows-2025
        os: windows
    stages:
      - stage: build
        displayName: Build
        jobs:
          - job: "Node"
            pool:
              name: "Azure-Pipelines-DevTools-EO"
              image: "ubuntu-latest"
              os: linux
            timeoutInMinutes: 90
            templateContext:
              outputs:
                - output: pipelineArtifact
                  displayName: "Upload NPM Package Artifact"
                  targetPath: $(System.DefaultWorkingDirectory)/target/npm/qsharp
                  artifactName: NPM
                  condition: succeeded()
            steps:
              # common init steps
              - script: |
                  bash DumpNpmrc.sh .npmrc
                displayName: Dump .npmrc before npmAuthenticate

              - task: npmAuthenticate@0
                inputs:
                  workingFile: .npmrc

              - script: |
                  bash DumpNpmrc.sh .npmrc
                displayName: Dump .npmrc after npmAuthenticate

              - task: npmAuthenticate@0
                inputs:
                  workingFile: source/npm/qsharp/.npmrc

              - task: RustInstaller@1
                inputs:
                  rustVersion: ms-prod-$(RUST_TOOLCHAIN_VERSION)
                  additionalTargets: wasm32-unknown-unknown
                  cratesIoFeedOverride: $(cratesIoFeedOverride)
                  toolchainFeed: $(toolchainFeed)
                displayName: Install Rust toolchain

              - task: PipAuthenticate@1
                displayName: Pip Authenticate
                inputs:
                  artifactFeeds: "AzureQuantum/azure-quantum"

              - task: UsePythonVersion@0
                inputs:
                  versionSpec: "3.11"
                  githubToken: "$(GH_PACKAGE_READ_TOKEN)"

              - task: UseNode@1
                inputs:
                  version: "22.x"

              - script: |
                  python ./prereqs.py --install && python ./version.py
                displayName: Install Prereqs and set version

              # build steps

              - script: |
                  python build.py --npm --wasm --no-check-prereqs
                displayName: Build npm package

              - script: |
                  bash DumpYocto.sh
                displayName: Dump yocto from npm cache

              - script: |
                  mkdir -p $(System.DefaultWorkingDirectory)/target/npm/qsharp
                  npm pack --pack-destination $(System.DefaultWorkingDirectory)/target/npm/qsharp
                displayName: Pack NPM Package
                workingDirectory: $(System.DefaultWorkingDirectory)/source/npm/qsharp

      - stage: approval
        displayName: Approval
        dependsOn: build
        condition: and(succeeded(), eq(variables['Build.Reason'], 'Manual'))
        jobs:
          - job: "Approval"
            pool: server
            timeoutInMinutes: 1440 # job times out in 1 day
            steps:
              - task: ManualValidation@0
                timeoutInMinutes: 1440 # task times out in 1 day
                inputs:
                  notifyUsers: ""
                  instructions: "Please verify artifacts and approve the release"
                  onTimeout: "reject"

      - stage: release
        displayName: Release
        dependsOn: approval
        condition: and(succeeded(), eq(variables['Build.Reason'], 'Manual'))
        jobs:
          - job: "Publish_VSIX_Package"
            pool:
              name: "Azure-Pipelines-DevTools-EO"
              image: "ubuntu-latest"
              os: linux
            templateContext:
              type: releaseJob
              isProduction: true
              inputs: # All input build artifacts must be declared here
                - input: pipelineArtifact
                  artifactName: VSIX
                  targetPath: $(System.DefaultWorkingDirectory)/target/vscode
            steps:
              - script: |
                  npm install -g @vscode/vsce
                displayName: Install Prereqs for VSCode Ext Publishing

              - script: |
                  VSIX_RPATH="$(System.DefaultWorkingDirectory)/target/vscode"
                  VSIX_FNAME=`ls $VSIX_RPATH/*.vsix`

                  VSIX_FNAME_NOEXT="${VSIX_FNAME%.vsix}"
                  MANIFEST_FILE="${VSIX_FNAME_NOEXT}.manifest"
                  SIGNATURE_FILE="${VSIX_FNAME_NOEXT}.signature.p7s"

                  echo "##vso[task.setvariable variable=VSIX_FNAME;]$VSIX_FNAME"
                  echo "##vso[task.setvariable variable=MANIFEST_FILE;]$MANIFEST_FILE"
                  echo "##vso[task.setvariable variable=SIGNATURE_FILE;]$SIGNATURE_FILE"
                displayName: Get VSCode Extension, Manifest, and Signature files for Publishing

              - script: |
                  echo "Command: vsce verify-signature --packagePath $VSIX_FNAME --manifestPath $MANIFEST_FILE --signaturePath $SIGNATURE_FILE"
                  vsce verify-signature --packagePath $VSIX_FNAME --manifestPath $MANIFEST_FILE --signaturePath $SIGNATURE_FILE
                condition: succeeded()
                displayName: Reconfirming signature verification before publishing

              - task: AzureCLI@2
                condition: and(succeeded(), eq(variables['BUILD_TYPE'], 'dev'))
                displayName: Publish pre-release VSCode Extension
                inputs:
                  azureSubscription: QDK Publish Connection
                  scriptType: bash
                  scriptLocation: inlineScript
                  inlineScript: |
                    echo "Command: vsce publish --pre-release --azure-credential --packagePath $VSIX_FNAME --manifestPath $MANIFEST_FILE --signaturePath $SIGNATURE_FILE"
                    vsce publish --pre-release --azure-credential --packagePath $VSIX_FNAME --manifestPath $MANIFEST_FILE --signaturePath $SIGNATURE_FILE 2>&1 > pub.log

              - task: AzureCLI@2
                condition: and(succeeded(), ne(variables['BUILD_TYPE'], 'dev'))
                displayName: Publish VSCode Extension
                inputs:
                  azureSubscription: QDK Publish Connection
                  scriptType: bash
                  scriptLocation: inlineScript
                  inlineScript: |
                    echo "Command: vsce publish --azure-credential --packagePath $VSIX_FNAME --manifestPath $MANIFEST_FILE --signaturePath $SIGNATURE_FILE"
                    vsce publish --azure-credential --packagePath $VSIX_FNAME --manifestPath $MANIFEST_FILE --signaturePath $SIGNATURE_FILE 2>&1 > pub.log

              - script: |
                  dir
                  cat pub.log
                displayName: If failed, display VSCode Publishing Results
                condition: failed()
                workingDirectory: "$(System.DefaultWorkingDirectory)"

          - job: "Publish_Python_Packages"
            pool:
              name: "Azure-Pipelines-DevTools-EO"
              image: "ubuntu-latest"
              os: linux
            templateContext:
              type: releaseJob
              isProduction: true
              inputs: # All input build artifacts must be declared here
                - input: pipelineArtifact
                  artifactName: Wheels.Win.x86_64
                  targetPath: $(System.DefaultWorkingDirectory)/artifacts/win-x86_64
                - input: pipelineArtifact
                  artifactName: Wheels.Win.aarch64
                  targetPath: $(System.DefaultWorkingDirectory)/artifacts/win-aarch64
                - input: pipelineArtifact
                  artifactName: Wheels.Mac.x86_64
                  targetPath: $(System.DefaultWorkingDirectory)/artifacts/mac-x86_64
                - input: pipelineArtifact
                  artifactName: Wheels.Mac.aarch64
                  targetPath: $(System.DefaultWorkingDirectory)/artifacts/mac-aarch64
                - input: pipelineArtifact
                  artifactName: Wheels.Linux.x86_64
                  targetPath: $(System.DefaultWorkingDirectory)/artifacts/linux-x86_64
                - input: pipelineArtifact
                  artifactName: Wheels.Linux.aarch64
                  targetPath: $(System.DefaultWorkingDirectory)/artifacts/linux-aarch64
                - input: pipelineArtifact
                  artifactName: Wheels.PlatformAgnostic
                  targetPath: $(System.DefaultWorkingDirectory)/artifacts/platform-agnostic
            steps:
              - script: |
                  mkdir -p $(System.DefaultWorkingDirectory)/target/wheels
                  find $(System.DefaultWorkingDirectory)/artifacts -name "*.whl" -exec cp {} $(System.DefaultWorkingDirectory)/target/wheels/ \;
                  ls $(System.DefaultWorkingDirectory)/target/wheels
                displayName: Collect and Display Py Artifacts in Publishing Dir

              - task: EsrpRelease@9
                condition: succeeded()
                displayName: Publish Py Packages
                inputs:
                  connectedservicename: "PME ESRP Azure Connection"
                  usemanagedidentity: true
                  keyvaultname: "quantum-esrp-kv"
                  signcertname: ESRPCert
                  clientid: "832c049d-cd07-4c1c-bfa5-c07250d190cb"
                  contenttype: PyPi
                  domaintenantid: "975f013f-7f24-47e8-a7d3-abc4752bf346"
                  folderlocation: "$(System.DefaultWorkingDirectory)/target/wheels"
                  waitforreleasecompletion: true
                  owners: "billti@microsoft.com"
                  approvers: "billti@microsoft.com"
                  mainpublisher: ESRPRELPACMAN

          - job: "Publish_NPM_Package"
            pool:
              name: "Azure-Pipelines-DevTools-EO"
              image: "ubuntu-latest"
              os: linux
            templateContext:
              type: releaseJob
              isProduction: true
              inputs: # All input build artifacts must be declared here
                - input: pipelineArtifact
                  artifactName: NPM
                  targetPath: $(System.DefaultWorkingDirectory)/target/npm/qsharp
            steps:
              - script: |
                  ls $(System.DefaultWorkingDirectory)/target/npm/qsharp/*
                displayName: Display NPM Artifacts in Publishing Dir

              - task: EsrpRelease@9
                condition: succeeded()
                displayName: Publish NPM Package
                inputs:
                  connectedservicename: "PME ESRP Azure Connection"
                  usemanagedidentity: true
                  keyvaultname: "quantum-esrp-kv"
                  signcertname: ESRPCert
                  clientid: "832c049d-cd07-4c1c-bfa5-c07250d190cb"
                  contenttype: "NPM"
                  domaintenantid: "975f013f-7f24-47e8-a7d3-abc4752bf346"
                  folderlocation: "$(System.DefaultWorkingDirectory)/target/npm/qsharp"
                  waitforreleasecompletion: true
                  owners: "billti@microsoft.com"
                  approvers: "billti@microsoft.com"
                  mainpublisher: ESRPRELPACMAN
